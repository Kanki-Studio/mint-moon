import React, { ReactNode } from "react";

import Page from "./home";

const Home = (): ReactNode => {
  return (
    <main className="w-full">
      <Page />
    </main>
  );
};

export default Home;
